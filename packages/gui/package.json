{
  "name": "@bobby/gui",
  "version": "0.1.0",
  "description": "Bobby GUI application",
  "author": "Bobby",
  "type": "module",
  "main": "dist-electron/main.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/bpc-oss/bobby.git",
    "directory": "packages/gui"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build && tsup electron/main.ts --format esm --out-dir dist-electron --external electron && tsup electron/preload.ts --format cjs --out-dir dist-electron --external electron",
    "package": "pnpm build && node -e \"const fs=require('node:fs');const path=require('node:path');const {spawnSync}=require('node:child_process');const src=path.join(process.cwd(),'electron-builder.yml');const tmp=path.join(process.cwd(),'electron-builder.package.local.yml');let status=1;const clean=()=>{try{fs.unlinkSync(tmp);}catch{}};try{const lines=fs.readFileSync(src,'utf8').split(/\\r?\\n/);const out=[];let skipping=false;for(const line of lines){if(!skipping&&/^publish:\\s*$/.test(line)){skipping=true;continue;}if(skipping){if(line.length===0||/^[^\\s]/.test(line)){skipping=false;} else {continue;}}if(!skipping) out.push(line);}fs.writeFileSync(tmp,out.join('\\n'));const result=spawnSync(process.execPath,[require.resolve('electron-builder/out/cli/cli.js'),'--config',tmp,'--publish','never'],{stdio:'inherit'});status=result.status ?? 0;} finally {clean();}process.exit(status);\"",
    "package:publish": "pnpm build && electron-builder --config electron-builder.yml --publish onTag",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@bobby/kernel": "workspace:*",
    "@bobby/shared": "workspace:*",
    "@codemirror/lang-markdown": "^6.5.0",
    "@codemirror/language": "^6.12.3",
    "@codemirror/state": "^6.6.0",
    "@codemirror/view": "^6.43.1",
    "@uiw/react-codemirror": "^4.25.10",
    "@vitejs/plugin-react": "^4.3.0",
    "electron-updater": "^6.8.3",
    "lucide-react": "^1.17.0",
    "react": "^18.3.0",
    "react-dom": "^18.3.0",
    "react-markdown": "^10.1.0",
    "rehype-harden": "^1.1.8",
    "remark-gfm": "^4.0.1",
    "streamdown": "^2.5.0",
    "vite": "^5.3.0",
    "zod": "^3.23.0",
    "zustand": "^5.0.14"
  },
  "devDependencies": {
    "@testing-library/react": "^16.0.0",
    "@types/react": "^18.3.0",
    "@types/react-dom": "^18.3.0",
    "autoprefixer": "^10.5.0",
    "electron": "^31.0.0",
    "electron-builder": "24.13.3",
    "jsdom": "^24.0.0",
    "postcss": "^8.5.15",
    "tailwindcss": "3"
  }
}
